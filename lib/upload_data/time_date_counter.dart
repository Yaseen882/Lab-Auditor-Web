import 'package:flutter/material.dart';

class UploadDataTimeCounter extends StatelessWidget {
  const UploadDataTimeCounter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Container();
  }
}
